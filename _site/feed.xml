<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2025-11-23T21:34:17+00:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Luis Pereira</title><subtitle>Notes Regarding DB&apos;s Etc</subtitle><entry><title type="html">Delta Lake Atomicity</title><link href="http://localhost:4000/delta-lake/2025/11/21/delta-table-atomicity.html" rel="alternate" type="text/html" title="Delta Lake Atomicity" /><published>2025-11-21T00:00:00+00:00</published><updated>2025-11-21T00:00:00+00:00</updated><id>http://localhost:4000/delta-lake/2025/11/21/delta-table-atomicity</id><content type="html" xml:base="http://localhost:4000/delta-lake/2025/11/21/delta-table-atomicity.html"><![CDATA[<p>I wanted to clarify to myself how we achieve Atomicity in Delta Lake and it’s easier for me to compare it to traditional databases.</p>

<p>Atomicity means that when we commit a transaction it’s an all-or-nothing operation. Either the transaction succeeds or it fails, nothing in between. Consistency means that despite our transaction being atomic, if some database constraint (e.g., a foreign key) is violated, then the operation is rejected and rolled back in spite of the commits being atomic. This distinction is important and will become clear why later. Atomicity pertains to the mechanism, whereas consistency checks if the transactions make logical sense given the constraints of the data model.</p>

<p>Let’s first cover how “traditional” databases like MySQL and Postgres achieve atomicity with the WAL. It’s best understood from the perspective of <strong>recovery</strong>:</p>

<p>We make changes to pages in memory - they can be flushed to disk at unpredictable times. Imagine: we modify a page, the buffer pool keeps it in memory (maybe it’s pinned), and the DB crashes before it’s written to disk. That means our commit was not durable - the user received a success message but the page was still in RAM.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Traditional DB - Without WAL]

Transaction modifies Page X in buffer pool
  ↓
User gets "COMMIT SUCCESS"
  ↓
💥 CRASH (before page flushes)
  ↓
Page X changes lost forever
</code></pre></div></div>

<p>By writing the WAL (with <code class="language-plaintext highlighter-rouge">fsync</code>) to disk first before acknowledging the COMMIT, the buffer pool can flush pages whenever it wants (following something like LRU-K). If the DB crashes, we simply replay the log, ensuring no partial writes.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Traditional DB - With WAL]

1. Modify Page X in memory
2. Write changes to WAL + fsync  ✓
3. Return "COMMIT SUCCESS" to user
4. (Later) Flush Page X to disk
   
If crash happens at step 4:
→ Replay WAL on restart
→ Page X changes restored
</code></pre></div></div>

<p>So how does the <a href="https://dennyglee.com/2023/08/31/what-is-the-delta-lake-transaction-log/">transaction log</a> in the Delta format give us atomicity? After all, it’s not a WAL, is it?</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Delta Lake Architecture]

Compute Engine (Spark/DuckDB/Trino)
  ↓
Delta Libraries (embedded)
  ↓
Object Storage (S3/GCS/ADLS)
</code></pre></div></div>

<p>In the Delta format, we have a compute engine (e.g., Spark, DuckDB, Trino). The engine manages data in memory, reading and writing Parquet files. The Delta libraries are embedded in the engine to intercept and interact with the storage layer (our proverbial disk) - in this case, distributed object storage (S3, GCS, ADLS, etc.).</p>

<p><strong>Here’s the key difference: There is no recovery… what?</strong></p>

<p>The compute engine does not update pages in place, so there’s no need to worry about unpredictable page flushing to disk. The Parquet files are rewritten to storage (added not ovewriting the existing ones), and a JSON log entry is added describing the write operation (copy-on-write). The JSON log entry says:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Remove: file_A.parquet</code> ← The one we modified</li>
  <li><code class="language-plaintext highlighter-rouge">Add: file_B.parquet</code> ← The new one with modified data</li>
</ul>

<p>Therefore, if there’s a crash (typically some network error), either the JSON entry exists or it doesn’t. Even if the new Parquet files exist, they are logically invisible (orphaned) to the compute engine. Thus, the sequence is opposite to traditional databases: we write the Parquet files first, and only then write our JSON log.</p>

<p>When we write to a single file such storage systems the write is guaranteed to be atomic. This is crucial as we will see later when we discuss <a href="#">isolation</a> in delta lakes.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Delta Lake Commit Sequence]

1. Write file_B.parquet (new data) ✓
2. Write JSON log entry atomically  ✓
   {
     "remove": {"path": "file_A.parquet"},
     "add": {"path": "file_B.parquet"}
   }
3. Return "COMMIT SUCCESS"

If crash at step 1:
→ file_B.parquet orphaned
→ No log entry → logically invisible
→ No recovery needed

Object storage guarantees:
Single file write is atomic ✓
</code></pre></div></div>

<p>This opens a new question! How do we handle this seemingly explosive storage growth? That’ll be the next note.</p>]]></content><author><name></name></author><category term="Delta-Lake" /><summary type="html"><![CDATA[I wanted to clarify to myself how we achieve Atomicity in Delta Lake and it’s easier for me to compare it to traditional databases.]]></summary></entry><entry><title type="html">How Delta Format Achieves Isolation</title><link href="http://localhost:4000/delta-lake/2025/11/21/delta-format-isolation.html" rel="alternate" type="text/html" title="How Delta Format Achieves Isolation" /><published>2025-11-21T00:00:00+00:00</published><updated>2025-11-21T00:00:00+00:00</updated><id>http://localhost:4000/delta-lake/2025/11/21/delta-format-isolation</id><content type="html" xml:base="http://localhost:4000/delta-lake/2025/11/21/delta-format-isolation.html"><![CDATA[<p>There is a very heavy penalty to pay in Delta Lake when transactions conflict. This note explores how the delta format achieves isolation between transactions.</p>

<p>In the best case scenario we enjoy lock free concurrency, in the worst case we waste compute and storage. That is the consequence of how concurrency is achieved with the Delta format - Optimistic Concurrency Control (OCC).</p>

<p>At a high level OCC consists of 5 stages:</p>

<ol>
  <li>Read phase (no locking)</li>
  <li>Modification phase (isolated work)</li>
  <li>Write <code class="language-plaintext highlighter-rouge">parquet</code> files (copy-on-write)</li>
  <li>Commit phase (serialisation point - only one wins)</li>
  <li>Conflict resolution (reconcile or retry)</li>
</ol>

<p>We should keep in mind that writing a single file to object storage is atomic - the file either exists or it doesn’t, never partially. This property, guaranteed by S3/Azure/GCS, is what makes the commit phase work.</p>

<p>Let’s imagine two simultaneous transactions (A &amp; B) attempting to modify a table: mf_doom_greatest_hits.</p>

<ol>
  <li>Both can read the parquet file referenced by <code class="language-plaintext highlighter-rouge">00000.json</code> (transaction log)</li>
  <li>They perform separate in memory transformations (the contents of the parquet file are copied into memory)</li>
  <li>They both write their new versions of the <code class="language-plaintext highlighter-rouge">parquet</code> file to storage</li>
</ol>

<p>Everything up to this point has happened without coordination. However, at step 4 both attempt to write to the JSON transaction log (atomically guaranteed - there can only be one winner):</p>

<ul>
  <li>Transaction A attempts to write <code class="language-plaintext highlighter-rouge">00001.json</code> → succeeds</li>
  <li>Transaction B attempts to write <code class="language-plaintext highlighter-rouge">00001.json</code> → fails (file exists)</li>
</ul>

<p>Assuming there are <strong>irreconcilable differences</strong> (when both transactions modify overlapping data files - for example, both updating rows in the same partition), Transaction B pays a heavy price: It has wasted the compute for modifying and writing a parquet file that will never be referenced.</p>

<p>This trade-off might seem harsh - wasted compute for simple coordination. But Delta Lake targets analytical workloads with large batch jobs, not high-concurrency OLTP with frequent small updates. The occasional wasted computation is a fair price for avoiding the complexity of fine-grained locking mechanisms like MVCC. Platforms like Databricks are offering <a href="https://www.databricks.com/blog/deep-dive-how-row-level-concurrency-works-out-box">row level concurrency</a>) but it’s not a Delta format native feature.</p>

<p>If the differences are reconcilable Transaction B just writes <code class="language-plaintext highlighter-rouge">00002.json</code> immediately. In such contexts OCC is fantastic there’s no coordination required in the compute heavy phases. Coordination is only needed in the cheapest step (writing metadata to JSON) where the first sign of conflict appears. Conflicts in analytical work loads are rare  (that’s why it’s an optimistic mechanism).</p>
<h2 id="why-conflicts-are-rare">Why Conflicts Are Rare</h2>

<p>So what makes conflicts in analytical work loads rare?</p>

<p>Analytical workloads usually don’t work on overlapping data:</p>

<ul>
  <li>Incremental loading (rarely modifying previous files)</li>
  <li>Generally low write concurrency it’s read heavy (BI Reports etc)</li>
  <li>Workloads are typically scheduled to not overlap time partitions (Overnight batch jobs)</li>
</ul>

<p>In general analytical workloads are far more controlled and predictable. Unlike the random nature of OLTP workloads. for example, many concurrent transactions attempting to decrement the same value (think Amazon shopping carts).</p>

<p>At this stage that covers how the delta format adds the I in ACID atop a Data Lake. However, I’d like to categories the types of conflicts and how they are resolved.</p>

<p>I’m surprised at how simple concurrency control is in Delta Lakes compared to say #MVCC . It’s almost too easy but given the analytical workloads it makes sense.</p>]]></content><author><name></name></author><category term="Delta-Lake" /><summary type="html"><![CDATA[There is a very heavy penalty to pay in Delta Lake when transactions conflict. This note explores how the delta format achieves isolation between transactions.]]></summary></entry></feed>